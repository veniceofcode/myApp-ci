apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    eventlistener:  myapp-github-listener
  name: myapp-github-listener-el
spec:
  port:
    targetPort: http-listener
  to:
    kind: Service
    name: el-myapp-github-listener